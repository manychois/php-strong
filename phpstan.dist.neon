parameters:
    level: max
    paths:
        - src
    treatPhpDocTypesAsCertain: false
    ignoreErrors:
        -
            # Public API for consumers of this library; no class inside src/ uses it.
            identifier: trait.unused
            path: src/Events/StoppableEventTrait.php
